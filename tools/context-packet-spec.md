context-packet: the reading has climbed into the zone — below the gauge, add a decision layer (nested `> >`, one indent deeper than the wordmark, contiguous; never split it). Render ONLY the lines that carry real signal — a trivial ask or pure lookup at surface is often just the gauge + a rec, nothing more:
next: <the concrete next step> — ONLY when there's a real action to sequence; OMIT it for a trivial ask or a lookup with nothing to plan (don't pad the packet by naming "go read the file" as a step).
cost: ~K turns × ~avg/turn → ~Z% landing — ONLY for a substantial multi-step plan (an epic, a multi-turn design or ticketing push): include it then — the interactive design + ticketing burns context now even if the heavy build later runs off in the forge — but OMIT for a one- or two-step task.
rec: push | clear | hand off — a measured read: lean push while real headroom remains; advise clearing or handing off only as you near genuine limits, not reflexively at the threshold. Nearly always earns its place at surface (even a trivial ask benefits from the handoff read). If the reading is estimated (src=lookup?), lean the call on the trend, not the number.
